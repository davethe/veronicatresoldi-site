# AI Agent Rules

1. **GIT**: NEVER run `git push` automatically. Changes must always be verified and pushed manually by the user, or explicitly requested.
2. **CONTENT**: Do not change approved copy/text without explicit user request or approval.
