# AI Agent Rules

1. **GIT**: NEVER run `git push` or `git commit` automatically. Always leave changes in the working directory for user review.
2. **CONTENT**: Do not change approved copy/text without explicit user request or approval.
3. **REVIEW**: The user MUST be able to see all changes (unstaged/staged) before any commitment or deployment.
